# Cloudflare Tunnel Token
# Get from: Cloudflare Dashboard → Networks → Tunnels → Your Tunnel → Token
# Then run: docker compose -f cloudflare-tunnel/docker-compose.yml up -d
CLOUDFLARE_TUNNEL_TOKEN=YOUR_CLOUDFLARE_TUNNEL_TOKEN_HERE
