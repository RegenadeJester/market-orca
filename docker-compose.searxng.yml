services:
  searxng:
    image: searxng/searxng:latest
    container_name: market-orca-searxng
    ports:
      - "127.0.0.1:18080:8080"
    volumes:
      - ./searxng:/etc/searxng
    environment:
      - SEARXNG_BASE_URL=http://127.0.0.1:18080/
    restart: unless-stopped
