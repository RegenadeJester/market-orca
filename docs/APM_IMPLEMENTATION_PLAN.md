# APM Implementation Plan — Report + Market Orca

## Source
- `/home/dicky/.openclaw/workspace/docs/APM.MD`
- Active slice: `2026-06-14 — Smart Alert Threshold dari Report Insight`

## Task Table

| Feature | Scope | Target files | Status | Verification |
|---|---|---|---|---|
| Personalized Report Preferences UI | GET/PUT prefs; settings page for tone/depth/language/topics/assets/Discord spam level | `backend/src/db.js`, `backend/src/server.js`, `frontend/src/pages/ReportPreferencesPage.vue`, `frontend/src/main.js`, `frontend/src/App.vue` | DONE | `node --check backend/src/server.js`; `node --check backend/src/db.js`; `npm --prefix frontend run build` |
| User Intent Memory Block integration | Inject saved prefs into daily report prompt/text generation | `backend/src/ai-daily-report.js` | DONE | `node --check backend/src/ai-daily-report.js`; generated text includes `## User Context` |
| Report Send Queue + Retry | queue failed Discord/send jobs; expose health pending/failed slice + Delivery Dashboard queue panel | `backend/src/db.js`, `backend/src/server.js`, `backend/src/ai-daily-report.js`, `frontend/src/pages/DeliveryDashboardPage.vue` | DONE | `node --check backend/src/db.js`; `node --check backend/src/server.js`; `node --check backend/src/ai-daily-report.js`; `curl http://localhost:4567/api/report-health` shows `send_queue`; `npm --prefix frontend run build` |
| Alert Recommendation Engine | recommended up/down thresholds from volatility/history; one-click apply in asset threshold editor | `backend/src/server.js`, `frontend/src/pages/AssetPage.vue` | DONE | `node --check backend/src/server.js`; `npm --prefix frontend run build`; `PORT=4599 node backend/src/server.js` + `GET /api/assets/aapl` returns `alert_recommendation` |
| Report Comparison Mode UI | view compare report slugs; diff topics/headlines/stats/repeats | `backend/src/server.js`, `frontend/src/pages/ReportPage.vue` | DONE | `node --check backend/src/server.js`; `npm --prefix frontend run build`; `GET /api/report/:slug/compare/:otherSlug` returns diff |
| Executive Morning Brief Voice/Text | Generate 5-bullet `brief.md` + per-slug brief from report topics/impact watch | `backend/src/ai-daily-report.js` | DONE | `node --check backend/src/ai-daily-report.js`; import smoke generated `Executive Morning Brief`; `npm --prefix frontend run build` |
| Incident Severity API | Latest report incident list with severity, recovery status, customer impact, action | `backend/src/ai-daily-report.js`, `backend/src/server.js` | DONE | `node --check backend/src/server.js`; `node --check backend/src/ai-daily-report.js`; `curl -fsS http://localhost:4567/api/incidents` |
| Recovery Status Tracker | DB tabel `incident_status`; status transitions (detected→investigating→partial_recovery→resolved); status history per incident; manual/bulk update API | `backend/src/db.js`, `backend/src/ai-daily-report.js`, `backend/src/server.js` | DONE | `node --check backend/src/{db,ai-daily-report,server}.js`; `GET /api/incidents` returns `title_hash` + `status_history`; `POST /api/incidents/status/update` manual transition; `GET /api/incidents/:hash/history` timeline; `npm --prefix frontend run build` |
| **Market Holiday / No-Data Edge Handler** | marketCalendar.js; getAssetFreshness() per asset; data_freshness badge (live/stale/holiday/closed); Data Status section in report text & HTML; QA gate if >30% stale; `/api/market-calendar` endpoint | `backend/src/market-calendar.js`, `backend/src/server.js`, `backend/src/ai-daily-report.js` | **DONE** | `node --check backend/src/{market-calendar,server,ai-daily-report}.js`; `curl http://localhost:4567/api/market-calendar` returns 4 market statuses; text report has `## Data Status` section; HTML has `Data Status` box; freshness badges per item; `npm --prefix frontend run build` |
| **Source Reliability Score Frontend Trust Badge** | source-reliability.js; config map 30+ sources; `scoreSourceTrust(source, url)`; DB table `source_reliability`; `GET /api/source-trust`; `GET /api/source-trust/check`; inject `source_trust` into report response; trust badge (●/◐/○) on ReportPage; hero selection boost/penalty | `backend/src/source-reliability.js`, `backend/src/server.js`, `backend/src/ai-daily-report.js`, `frontend/src/pages/ReportPage.vue` | **DONE** | `node --check backend/src/{source-reliability,server,ai-daily-report,db}.js`; `GET /api/source-trust` returns 30 sources; `GET /api/source-trust/check?source=Reuters` → `{score:90,label:'high_trust'}`; `GET /api/report/:slug` includes `source_trust`; `npm --prefix frontend run build`; main server needs restart to deploy |
|| **Smart Alert Threshold dari Report Insight** | `extractAlertCandidates(topics)` → array {asset,targetPrice,direction,confidence}; DB tables `suggested_alerts` + `user_alert_feedback`; 5 API endpoints (list/generate/approve/reject/block); inject into report text; MAX_PENDING_ALERTS=5 per user | `backend/src/db.js`, `backend/src/ai-daily-report.js`, `backend/src/server.js` | **DONE** | `node --check backend/src/{db,ai-daily-report,server}.js`; `GET /api/alerts/suggested` returns array; `POST /api/alerts/suggested/generate` returns `{inserted,skipped}`; `POST /api/alerts/suggested/:id/(approve|reject)` changes status; `GET /api/alerts/suggested/block` returns markdown; report text includes `## ⚡ Suggested Alerts` section; smoke test with mock topics yields 0-3+ candidates |
|| **Interactive Report Canvas (Backend)** | `report_canvas` + `export_jobs` tables; GET/POST canvas (section_order, overrides, notes, hidden_sections); MD/HTML/JSON export honoring canvas state; export cleanup cron | `backend/src/report-canvas.js`, `backend/src/server.js` | **DONE** | `node --check backend/src/{report-canvas,server}.js`; `GET /api/report/:slug/canvas` returns sections; `POST /api/report/:slug/canvas/save` persists reorder + notes; `GET /api/report/:slug/export?format=md\|html` returns file respecting hidden+reorder; `npm --prefix frontend run build` |
| **Report Health Status Cards** | Health metrics in `/api/report-health` (report age, delivery rate, queue health, deliverables completeness, overall status); visual status cards in DeliveryDashboardPage (system health dot, last report age, delivery rate %, deliverables ok/total, queue item counts) | `backend/src/server.js`, `frontend/src/pages/DeliveryDashboardPage.vue` | **DONE** | `node --check backend/src/server.js`; `npm --prefix frontend run build`; `GET /api/report-health` returns `health` object with `overall_status/reason/report_age_hours/delivery_success_rate/queue_pending/queue_failed/deliverables_ok/total_reports`; DeliveryDashboardPage renders 5 status cards with color-coded indicators |

## Next backlog items (not yet picked)
1. Catch-up Report Engine (Batch 1, #1)
2. Discord Digest Mode improvements (Batch 1, #2 — partial)
3. Market Impact Simulator v2 — custom event enhancements (Batch 1, #7 — partial)
4. ~~Report health frontend status cards (Delivery Health Dashboard — partial)~~ → DONE (2026-06-17)
